# Return statement :
#_Its used to return a value outside the function when return is executed control goes back to the caller
#_Allways a single return is executed If we want to execute multiple return then we need to specify condition for it
#syntax :

def add( ) :
	print('add function')
	return 5
a = add( )
print(a)